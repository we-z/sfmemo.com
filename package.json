{
  "name": "sfmemo.com",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && python3 -m http.server 4173 -d dist/client",
    "build": "node build.mjs",
    "generate:chip-texture": "node scripts/generate-chip-texture.mjs"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@napi-rs/canvas": "0.1.100",
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  }
}
